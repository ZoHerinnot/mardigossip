<h1>Bienvenu sur notre super site de potin</h1>

<p>
<%@gossips.each do |gossip|%>
	<br>
	Auteur :
	  <% user = gossip.user_id %>
      <%=  @user.find(user).first_name %> <br> 
	Titre:
	<%=  gossip.title %> <%= link_to "details potin", controller:"gossips", action:"show", id: gossip.id %><br>
	 <a href="gossips/<%= gossip.id %>" class= "btn btn-danger" data-confirm= "Voulez-vous vraiment supprimer", data-method="DELETE">Supprimer</a>
<%end%> 
</p>
