<html>
  <body>
<!--
 <form action="update" method="PUT">
  <label for="title">Nouveau titre : </label>
  <input type="title" name="title" id="title" value="<%= @gossips.title %>"><br>
  <label for="content">Nouveau content :</label>
  <textarea name="content" id="content" ><%= @gossips.content %></textarea><br>
  <input type="submit" name="edit" value="Confirmer">

 </form>

-->
 <%= form_tag url_for(action: 'update'), method: "PUT" do %>
      <%= label_tag 'Title' %><br />
      <%= text_field_tag 'title',@gossips.title %>
      <br /> <br />

      <%= label_tag 'Content' %><br />
      <%= text_area_tag 'content', @gossips.content %>
      <br /> 
    <%= hidden_field_tag 'user_id', @gossips.user_id %>
  
      <%= submit_tag "Create gossip" %>

    <% end %>

    <% if @gossips.errors.any? %>
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
    <strong>Oups punaise!</strong> You should check in on some of those fields below.
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossips.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
  </body>
</html>